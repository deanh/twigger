# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'twigger' do
  developer('Dean Hudson', 'dean@ero.com')
  extra_deps << ['twitter', '~> 1.0']
  extra_deps << ['activerecord', '~> 2.3']
  extra_deps << ['mysql', '~> 2.8']
  # self.rubyforge_name = 'twiggerx' # if different than 'twigger'
end
